# HackBuddy Frontend

React (Vite) frontend for HackBuddy Teammate Match.

## Setup
1. `npm install`
2. Configure `.env` with `VITE_API_BASE_URL` and `VITE_GOOGLE_CLIENT_ID`.
3. `npm run dev`

## Deployment
Ready for Vercel out of the box using `vercel.json`.
